import Link from 'next/link';

export default ({ menu }) => (
    <div>
        <div class="logo">
            <h2>Next Movie</h2>
            <style>{`
              .logo {
                  display: flex;
                  flex-wrap: wrap;
                  justify-content: center;
              }
            `}</style>
        </div>
    </div>
)